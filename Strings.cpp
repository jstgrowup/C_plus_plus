#include <iostream>
#include <string>
using namespace std;
int main()
{
    string name = "subham";
    cout << "the name is " << name.length() << endl;
    cout << "the name is " << name.substr(0, 3);

    return 0;
}